rec Form {
    first_name: String,
    last_name: String,
    age: Number,
}

enum UserRole {
    Admin,
    Developer,
    NormalUser
}

rec User {
    email: String,
    password: String,
    roles: Vec<NormalUser>,
}