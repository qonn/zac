`import { h, Component } from '../deps.jsx'

export class Comments extends Component {
  render() {
    return (
      <ul>
        {this.props.comments.map((comment) => {
          return <li>{comment}</li>
        })}
      </ul>
    )
  }
}`