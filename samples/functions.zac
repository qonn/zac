fn a(b: String) {
}

fn no_arg() {
}

fn single_argument(a: Number) {
}

fn multi_arguments(a: String, b: String) {
}

fn js_literal() {
    `#{what}`
}

fn variable() {
    let a = "hello world"
}

fn variable_shadowing() {
    let a = "hello world 1"
    let a = "hello world 2"
}

fn function_calling() {
    let b = "1"
    a(b)
}

fn function_calling_multiline() {
    single_argument(
        (1 / 2) +
        bar(
            a,
            b,
            5 + 1
        )
    )
}

fn add(a: Number, b: Number) {
    a + b
}

fn number_to_string(what: Number) {
    `'#{what}'`
}

fn log(what: String) {
    `console.log(#{what})`
}

log(number_to_string(add(5, 10)))

fn do() {}
fn do2(what: Number) {}

fn if_statement() {
    let c = 123

    if a {
        do()
    } else {
        do2(c)
    }
}