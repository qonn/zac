enum Promise<resolve, reject> {
    Resolved(resolve),
    Rejected(reject)
}

enum Option<value> {
    Some(value),
    None,
}

enum Result<value, error> {
    Ok(value),
    Error(error),
}

enum Foo {
    Bar,
    Haz
}